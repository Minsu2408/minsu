Here’s a clean **Markdown summary** of your uploaded file **기초문법복습.pdf**:

````markdown
# 자바 기초 문법 복습 요약

## 1. 자바 프로그램의 기본 구조
- 클래스 선언: `class` 키워드 사용, `public`으로 공개 가능
- 주석: `//` (한 줄), `/* */` (여러 줄)
- `main()` 메소드: 프로그램 실행 시작점
- 메소드: C/C++의 함수에 해당, 클래스 내부에서만 정의 가능

## 2. 식별자와 네이밍 규칙
- 식별자: 클래스, 변수, 메소드 이름
- 규칙:
  - 숫자로 시작 불가
  - 예약어 사용 불가 (`if`, `null`, 등)
  - `_`, `$` 사용 가능
  - 대소문자 구분
- 네이밍 관습:
  - 클래스: 단어마다 첫 글자 대문자 (`HelloWorld`)
  - 변수/메소드: 첫 단어 소문자, 이후 단어는 대문자 (`myAge`, `getAge`)
  - 상수: 모두 대문자 (`PI`)

## 3. 데이터 타입
- 기본 타입 (8개): `boolean`, `char`, `byte`, `short`, `int`, `long`, `float`, `double`
- 참조 타입 (1개): 배열, 클래스, 인터페이스

## 4. 변수, 리터럴, 상수
- 변수: 메모리에 값 저장
- 리터럴: 직접 표현한 값 (`34`, `'A'`, `"hello"`, `true`)
- 상수: `final` 키워드 사용, 값 변경 불가
- 타입 변환:
  - 자동 변환 (작은 타입 → 큰 타입)
  - 강제 변환 `(타입) 값` → 데이터 손실 주의

## 5. 입력과 출력
- 출력: `System.out.println()`
- 입력: `Scanner` 클래스 사용
  ```java
  Scanner sc = new Scanner(System.in);
  int age = sc.nextInt();
````

## 6. 연산자

* 산술: `+ - * / %`
* 증감: `++ --`
* 비교: `== != > < >= <=`
* 논리: `&& || !`
* 조건 연산자: `조건 ? 참값 : 거짓값`
* 비트 연산: `& | ^ ~ << >>`

## 7. 조건문

* `if`, `if-else`, `else if`, 중첩 if
* `switch-case`: `int`, `char`, `String`만 사용 가능

## 8. 반복문

* `for`, `while`, `do-while`
* 제어문:

  * `break` (반복 종료)
  * `continue` (다음 반복으로 건너뜀)
* 중첩 반복: 구구단 출력 등

## 9. 배열

* 선언: `int[] arr = new int[5];`
* 초기화: `int[] arr = {1,2,3,4,5};`
* `length` 필드로 크기 확인
* `for-each` 반복문
* 다차원 배열: `int[][] arr = new int[2][3];`
* 비정방형 배열 지원

## 10. 메소드와 main()

* 메소드: 리턴 타입 + 이름 + 매개변수
* 배열 반환 가능 (`return 배열;`)
* `main(String[] args)` : 명령행 인자 전달 가능

## 11. 주요 예제

* 원의 면적 계산 (`PI * r^2`)
* 타입 변환 실습
* Scanner 입력 실습
* 학점 계산 (`if-else`, `switch`)
* 반복문으로 합계, 평균 구하기
* 구구단 출력 (중첩 for문)
* 배열 활용 (최대값, 평균 구하기)
* 명령행 인자 합계 프로그램

## 12. 실습 과제

1. 가위 바위 보 게임
2. Up & Down 숫자 맞추기 게임

👉 어떤 형태로 원하시나요?
```
